# JuPin 剧本杀拼车项目面试总结

> 定位：Java 后端实习项目。核心亮点不是简单 CRUD，而是围绕剧本杀拼车场景做了身份隔离、拼车状态机、押金/车费订单、信用分、匹配推荐、实时群聊和并发控制。

## 第一部分：项目整体介绍

JuPin 是一个面向剧本杀场景的拼车组局系统，核心目标是解决玩家找局、店家发局、DM 主持、押金约束、成团确认、群聊沟通和评价反馈这些真实业务问题。项目不是单纯的活动报名 CRUD，而是围绕“剧本杀拼车”这个垂直场景做了完整业务建模。

项目覆盖玩家、店家、管理员三类身份，并把“玩家局”和“店家局”拆成两套不同的业务规则。玩家局强调玩家自发组局、DM 可转让、系统匹配推荐；店家局强调店铺成员权限、店铺剧本库、店家发布和店铺收款。这样做的原因是剧本杀拼车不是单纯的活动报名，参与者、主持人、店铺和资金流向都有不同责任边界，如果全部用一个“活动表 + 报名表”处理，后续权限、支付和评价规则会混在一起，代码很难维护。

项目采用 Spring Boot 多模块结构，将公共能力、实体 DTO/VO、业务服务分离：

- `jupin-common`：公共异常、常量、JWT 工具、上下文。
- `jupin-pojo`：entity、dto、vo。
- `jupin-server`：controller、service、mapper、config、engine。
- `jupin-web`：Vue 3 前端 Demo。

这样做的好处是模块职责清晰，公共常量和异常不会散落在业务代码中，后续如果拆分服务或增加管理后台，也更容易复用模型和公共能力。

相似做法包括单模块 Maven 项目、按领域拆分 Maven 模块、微服务拆分。当前选择多模块单体，是因为项目规模还没到微服务阶段，但又需要体现分层和可维护性；多模块单体比微服务部署成本低，比单模块项目边界更清楚。

## 第二部分：项目技术栈

### 2.1 Spring Boot + MyBatis-Plus 分层开发

#### 2.1.1 为什么这么做

项目需要快速实现用户、店铺、剧本、拼车、订单、评价、消息等大量业务表的增删改查，同时也需要处理一些复杂查询和条件更新。Spring Boot 能提供成熟的 Web、事务、配置、依赖注入能力；MyBatis-Plus 能减少基础 CRUD 样板代码，同时保留 MyBatis 对 SQL 的可控性。

#### 2.1.2 使用了什么技术

- Spring Boot 2.7.x：应用启动、Controller、Service、事务、配置管理。
- MyBatis-Plus：BaseMapper、QueryWrapper、UpdateWrapper、分页插件、逻辑删除、自动填充。
- MySQL 8.0：核心关系型数据存储。

#### 2.1.3 相似功能的技术

- Spring Data JPA：更偏 ORM，适合实体关系稳定、查询较规范的项目。
- 原生 MyBatis：SQL 控制最强，但 CRUD 样板代码更多。
- JOOQ：类型安全 SQL 能力强，但学习和引入成本更高。

#### 2.1.4 为什么选择当前技术

剧本杀拼车系统有很多条件查询和状态更新，例如按城市、剧本类型、价格、开始时间筛选拼车，支付押金时还要按订单状态和成员状态做条件更新。MyBatis-Plus 比 JPA 更容易控制 SQL 行为，又比原生 MyBatis 少写大量基础 CRUD，适合实习项目在效率和可控性之间取平衡。

#### 2.1.5 运用在哪些模块

- `UserServiceImpl`：用户注册、登录、资料查询和更新。
- `PoolServiceImpl`：拼车创建、列表筛选、加入、退出、确认。
- `OrderServiceImpl`：订单创建、支付、退款、释放。
- `ShopServiceImpl` / `ScriptServiceImpl`：店铺和剧本管理。
- `MyMetaObjectHandler`：自动填充 `createTime`、`updateTime`。

### 2.2 JWT + Spring Security + 拦截器的身份认证

#### 2.2.1 为什么这么做

项目有玩家、店家、管理员三种身份，并且接口路径按角色划分为 `/api/player/**`、`/api/shop/**`、`/api/admin/**`。如果只在前端控制菜单，后端接口仍然可能被越权调用。因此需要后端在每次请求时解析登录态并校验角色。

JWT 的优势是无状态，后端不需要为每个登录用户保存 Session，适合前后端分离和后续横向扩展。项目还引入 Refresh Token 和 Redis 黑名单，用于续期和退出登录后的 Token 吊销。

#### 2.2.2 使用了什么技术

- Spring Security：提供安全配置和密码编码器。
- JWT：Access Token 携带 userId、phone、role 等声明。
- Redis：保存 Refresh Token 和 JWT 黑名单。
- HandlerInterceptor：解析请求头 Token，写入 `BaseContext`，并做角色路径校验。

#### 2.2.3 相似功能的技术

- Session + Cookie：传统服务端会话，适合强服务端渲染或单体应用。
- OAuth2 / Spring Authorization Server：适合开放平台和第三方授权。
- Sa-Token、Shiro：更轻量的权限框架。

#### 2.2.4 为什么选择当前技术

当前是前后端分离项目，JWT 更容易和 Vue 前端配合，也方便 WebSocket 建连时携带认证信息。OAuth2 对当前项目过重，Session 在多端和横向扩展时需要处理共享会话。Spring Security + JWT 能体现主流企业后端实践，同时实现成本可控。

#### 2.2.5 运用在哪些模块

- `AuthController`：登录、注册、刷新 Token、退出登录。
- `UserServiceImpl`：登录校验、Refresh Token 校验、JWT 黑名单写入。
- `JwtAuthInterceptor`：HTTP 请求鉴权和角色路径校验。
- `JwtAuthChannelInterceptor`：WebSocket STOMP 连接鉴权。
- `BaseContext`：保存当前用户 id 和角色，供业务层使用。

### 2.3 BCrypt 密码存储

#### 2.3.1 为什么这么做

密码不能明文存储，也不应该用普通 MD5/SHA 直接摘要。MD5 速度太快，攻击者拿到数据库后可以用彩虹表或 GPU 批量撞库。BCrypt 是专门用于密码存储的慢哈希算法，内置随机盐，并且可以通过强度因子控制计算成本。

#### 2.3.2 使用了什么技术

- `BCryptPasswordEncoder`
- 注册时调用 `passwordEncoder.encode(rawPassword)`。
- 登录时调用 `passwordEncoder.matches(rawPassword, encodedPassword)`。

#### 2.3.3 相似功能的技术

- MD5/SHA-256 + salt：比明文好，但仍然太快，不适合密码存储。
- PBKDF2：标准化程度高，可配置迭代次数。
- SCrypt：抗硬件破解能力较强。
- Argon2：现代密码哈希算法，安全性强，但环境兼容和依赖成本略高。

#### 2.3.4 为什么选择当前技术

BCrypt 是 Spring Security 生态里默认且成熟的选择，使用简单、兼容好、面试认可度高。相比 Argon2，它不需要额外引入复杂依赖；相比 MD5/SHA，它更适合抵抗离线暴力破解。

#### 2.3.5 运用在哪些模块

- `SecurityConfig`：注册 `PasswordEncoder` Bean。
- `UserServiceImpl.register`：注册时加密密码。
- `UserServiceImpl.login`：登录时进行密码匹配。

### 2.4 Redis + Redisson 并发控制

#### 2.4.1 为什么这么做

拼车项目存在典型并发场景：同一拼车最后一个名额可能被多人同时支付；多人同时确认成团可能重复计数；多个用户抢同一个剧本角色也不能重复占用。如果只在 Java 内存里加锁，服务多实例部署时锁不生效；如果只靠先查后改，也容易出现超员和重复占座。

#### 2.4.2 使用了什么技术

- Redis：作为分布式协调组件。
- Redisson `RLock`：对同一个 `poolId` 加分布式锁。
- MySQL 条件更新：订单、成员和名额更新都带原状态或容量条件。
- Redis Hash `putIfAbsent`：角色抢选时原子占位。
- `StringRedisTemplate`：操作 Token、角色、Geo 等字符串结构。

#### 2.4.3 相似功能的技术

- Java `synchronized` / `ReentrantLock`：只适合单 JVM。
- MySQL 悲观锁 `select ... for update`：一致性强，但数据库压力较大。
- MySQL 乐观锁 / 条件更新：适合库存扣减类场景。
- ZooKeeper / Etcd 分布式锁：一致性强，但部署和维护成本更高。
- Redis Lua 脚本：适合把多个 Redis 操作封装为原子逻辑。

#### 2.4.4 为什么选择当前技术

Redisson 封装了锁续期、可重入、释放校验等细节，比手写 `SETNX + EXPIRE` 更安全。项目本身已经使用 Redis 做 Token 和角色抢选，因此复用 Redis 做分布式锁成本低。

但分布式锁不能作为唯一防线，所以支付占座还增加了 MySQL 条件更新兜底：订单只能从 `PENDING -> PAID`，成员只能从 `PENDING_PAYMENT -> JOINED`，拼车名额通过 `current_members < max_members` 原子更新。这样即使出现重复请求、锁异常或并发竞争，数据库层也能阻止超员和重复占座。

#### 2.4.5 运用在哪些模块

- `PoolServiceImpl.join`：加入拼车时锁定 `poolId`。
- `PoolServiceImpl.confirm`：完成确认/结束确认时锁定 `poolId`，防止重复计数和状态并发流转。
- `OrderServiceImpl.payDeposit`：押金支付时锁定 `poolId`，并通过 MySQL 原子名额更新防止超员。
- `PoolServiceImpl.selectRole`：Redis Hash 抢选角色，保证同一个角色只能被一个用户占用。

### 2.5 拼车状态机

#### 2.5.1 为什么这么做

拼车生命周期不是简单的状态字段修改，而是有严格的流转规则：`OPEN` 可以变 `FULL`，`FULL` 经过全员确认后才能变 `COMPLETED`，`COMPLETED` 后不能再取消，只能进入 `FINISHED`。如果状态判断散落在各个 service 方法里，后续容易出现非法流转，例如未满员直接完成、已完成后又取消。

#### 2.5.2 使用了什么技术

- 独立的 `PoolStateMachine` 服务封装状态流转。
- 常量类 `PoolStatus`、`MemberStatus`、`ConfirmStatus` 表示枚举语义。
- 事务控制保证状态更新和关联业务操作一致。

#### 2.5.3 相似功能的技术

- Spring Statemachine：更完整的状态机框架。
- 事件溯源：记录所有事件，由事件重放得到状态。
- 数据库状态字段 + 业务条件判断：实现简单，但容易分散。

#### 2.5.4 为什么选择当前技术

当前状态数量有限，使用独立状态机类已经能把核心流转集中管理。Spring Statemachine 对当前项目偏重，会增加学习和配置成本；事件溯源更适合审计要求很高的大型系统。项目选择轻量状态机，既能体现设计意识，又不会过度工程化。

#### 2.5.5 运用在哪些模块

- `PoolStateMachine`：封装 `toCompleted`、`toFinished`、`toCancelled`、`rollbackToOpen` 等流转。
- `PoolServiceImpl`：加入、退出、确认、取消、完成流程调用状态机。
- `OrderServiceImpl`：支付后根据人数触发满员状态。

### 2.6 押金/尾款分阶段订单和订单幂等

#### 2.6.1 为什么这么做

剧本杀拼车涉及“押金”和“剩余车费”，押金不是额外费用，而是总费用的一部分。当前项目处于模拟支付阶段，没有接入真实支付网关，因此重点不是第三方资金清结算，而是把押金订单和尾款订单拆成两类业务状态，约束玩家从加入、占座、确认到超时处理的履约流程。

订单需要幂等，因为支付按钮可能重复点击，接口也可能因为网络抖动被重试。如果不做幂等和状态校验，可能重复创建订单、重复支付或重复改变成员状态。

#### 2.6.2 使用了什么技术

- 订单号使用 Hutool Snowflake 生成。
- `order.type` 区分押金订单和车费订单。
- `order.status`、`release_status` 控制支付和释放状态。
- `user_id + idempotent_key` 唯一索引约束重复创建订单。
- 支付押金时使用 Redisson 锁 + MySQL 名额原子更新 + 订单状态校验 + 成员状态条件更新。
- 支付成功后按正式成员数反算 `current_members`，修复简单自增可能带来的计数漂移。

#### 2.6.3 相似功能的技术

- UUID 订单号：实现简单，但不如雪花 ID 有序。
- 数据库自增 ID 直接做订单号：暴露业务规模，不适合作外部订单号。
- 消息队列异步支付状态机：更适合真实支付回调和高并发交易。
- TCC / Seata 分布式事务：适合跨服务强一致交易，当前项目偏重。

#### 2.6.4 为什么选择当前技术

当前是 Mock 支付，不需要引入完整支付网关和分布式事务。Snowflake 能生成趋势递增、全局唯一的订单号；本地事务 + 状态幂等 + 分布式锁 + 数据库条件更新足以覆盖当前阶段的支付占座一致性。

#### 2.6.5 运用在哪些模块

- `OrderServiceImpl.create`：创建押金/车费订单。
- `OrderServiceImpl.pay` / `payDeposit`：模拟支付和押金支付后的成员转正。
- `OrderServiceImpl.refund` / `release`：退款与资金释放。
- `PoolServiceImpl.cancel`：拼车取消后关联订单退款。

### 2.7 匹配引擎和策略模式

#### 2.7.1 为什么这么做

剧本杀拼车的核心差异化能力是“帮玩家找到合适的局”。匹配不能只按一个字段筛选，而要综合城市、时间、剧本偏好、信用分等维度。如果所有打分逻辑写在一个方法里，后续新增“距离”“熟人避让”“性别比例”“历史偏好”等规则会很难维护。

#### 2.7.2 使用了什么技术

- `MatchEngine`：统一调度候选用户和策略打分。
- `MatchStrategy` 接口：每个策略独立实现 `score` 和 `weight`。
- 策略实现包括城市、时间窗口、偏好、信用等。
- 定时任务 `MatchTask` 每分钟扫描开放拼车并输出推荐结果。

#### 2.7.3 相似功能的技术

- SQL 排序：简单筛选适合，但复杂权重难维护。
- 规则引擎 Drools：适合大量业务规则，但当前项目过重。
- 机器学习推荐：效果上限高，但需要数据积累和训练成本。
- Elasticsearch function score：适合搜索推荐，但需要额外基础设施。

#### 2.7.4 为什么选择当前技术

策略模式轻量、可测试、可扩展，适合当前没有大量历史数据的阶段。相比规则引擎和机器学习，它更容易解释推荐结果，也更适合面试中说明设计思路。

#### 2.7.5 运用在哪些模块

- `MatchEngine`：匹配主流程。
- `MatchStrategy` 及其实现类：城市、时间、偏好、信用打分。
- `MatchTask`：定时扫描开放拼车。
- `PlayerPreferenceServiceImpl`：维护玩家偏好，为匹配提供依据。

### 2.8 WebSocket STOMP 实时通信

#### 2.8.1 为什么这么做

拼车确认、超时取消、订单逾期、聊天消息都需要较强的实时性。如果只靠前端轮询，会增加接口压力，并且用户体验较差。WebSocket 能让服务端主动向客户端推送拼车维度的状态刷新事件。

#### 2.8.2 使用了什么技术

- Spring WebSocket。
- STOMP 消息协议。
- `/topic/pool/{poolId}` 作为拼车状态和确认事件频道。
- `/topic/pool/{poolId}/chat` 作为拼车群聊频道。
- WebSocket ChannelInterceptor 做 JWT 鉴权。

#### 2.8.3 相似功能的技术

- HTTP 轮询：实现简单，但延迟和请求量较高。
- Server-Sent Events：适合服务端单向推送，不适合双向聊天。
- MQTT：适合 IoT 和移动端弱网推送，但 Web 业务接入成本更高。
- 第三方 IM 服务：功能强，但依赖外部平台。

#### 2.8.4 为什么选择当前技术

项目需要双向聊天和服务端通知，WebSocket 比 SSE 更适合；STOMP 在 Spring 生态中支持成熟，可以用订阅路径区分不同拼车频道。相比自定义裸 WebSocket 协议，STOMP 的消息语义更清楚。

#### 2.8.5 运用在哪些模块

- `WebSocketConfig`：STOMP 端点和消息代理配置。
- `JwtAuthChannelInterceptor`：WebSocket 连接鉴权。
- `ChatServiceImpl`：聊天消息落库和发送。
- `PoolServiceImpl`：发起确认、确认结果变化时推送拼车事件。
- `TimeoutConsumer`：订单逾期、拼车超时取消、确认兜底成功后推送刷新事件。

### 2.9 信用分和评价分双体系

#### 2.9.1 为什么这么做

剧本杀拼车有两个不同评价维度：玩家是否守约，以及 DM/店铺服务质量。如果只用一个评分，会把“跳车违约”和“服务体验差”混在一起。项目拆成信用分和评价分，信用分约束玩家履约行为，评价分约束 DM 和店铺服务质量。

#### 2.9.2 使用了什么技术

- `credit_score` 存在用户表中。
- `credit_log` 记录每次加减分原因和余额。
- `review` 表记录玩家对店家或 DM 的评分。
- 服务层事务保证扣分和日志写入一致。

#### 2.9.3 相似功能的技术

- 单一用户评分：实现简单，但表达能力弱。
- 标签体系：可以补充评价维度，但不适合做硬约束。
- 风控规则引擎：适合复杂处罚策略，当前阶段偏重。

#### 2.9.4 为什么选择当前技术

双体系能贴近真实业务：守约行为由系统自动判断，服务体验由玩家主观评价。这样既能防止玩家频繁跳车，也能防止低评分 DM 继续接单。

#### 2.9.5 运用在哪些模块

- `CreditServiceImpl`：信用分增减和日志记录。
- `PoolServiceImpl.leave`：COMPLETED 后跳车扣信用分。
- `ReviewServiceImpl`：评价提交和查询。
- `PoolServiceImpl.assignDm`：指派 DM 时可根据评价分限制。

### 2.10 RabbitMQ 死信超时处理

#### 2.10.1 为什么这么做

订单支付超时、拼车开始超时、成团确认超时和结束确认超时都不适合放在主交易链路里同步等待。如果用定时任务轮询，扫描粒度和延迟不好控制；如果依赖前端轮询，又无法保证异常流程一定被处理。项目使用 RabbitMQ 延迟队列 + 死信队列，把超时任务从主链路中解耦，到期后由消费者重新查库并按状态机处理。

#### 2.10.2 使用了什么技术

- Spring AMQP。
- RabbitMQ 延迟队列 + 死信交换机 + 统一消费队列。
- `TimeoutProducer`：按业务类型投递延迟消息。
- `TimeoutConsumer`：消费死信消息并处理订单/拼车/确认超时。
- `application.yml` 中配置 RabbitMQ host、端口、账号、监听并发和 prefetch。

#### 2.10.3 相似功能的技术

- Redis Stream：轻量，适合已有 Redis 的项目。
- Kafka：吞吐高，适合日志和大规模事件流。
- RocketMQ：事务消息和延迟消息能力强，国内业务常用。
- 本地线程池异步：实现简单，但可靠性差，服务重启会丢任务。

#### 2.10.4 为什么选择当前技术

RabbitMQ 更适合业务事件驱动和可靠消息投递，路由能力清晰，Spring Boot 集成成熟。相比 Kafka，它对当前这种订单超时、状态变更、通知推送类业务更轻量；相比本地线程池异步，它能在服务重启后保留队列中的补偿任务。

#### 2.10.5 运用在哪些模块

- 订单押金超时：`PENDING -> OVERDUE`，成员待支付状态回退为退出。
- 尾款超时：订单逾期并扣减信用分。
- 拼车开始超时：到开始时间仍无人加入时自动取消。
- 成团确认/结束确认超时：满足条件时推进 `COMPLETED` / `FINISHED`。
- 超时处理成功后写入站内消息，并通过 `/topic/pool/{poolId}` 推送刷新事件。

### 2.11 前端 Vue 3 工程化

#### 2.11.1 为什么这么做

项目不仅有后端接口，也需要一个可演示的 Web Demo，展示玩家端、店家端、管理员端的核心流程。Vue 3 + Vite 能快速构建 SPA，Element Plus 提供成熟的表单、表格、弹窗和布局组件。

#### 2.11.2 使用了什么技术

- Vue 3
- Vite
- TypeScript
- Element Plus
- Pinia
- Vue Router
- Axios

#### 2.11.3 相似功能的技术

- React + Ant Design：生态强，但项目已有 Vue 风格更适合快速开发。
- Angular：工程规范强，但学习和开发成本更高。
- 原生 HTML/JS：简单页面可以，但难维护复杂状态。

#### 2.11.4 为什么选择当前技术

Vue 3 学习成本较低，适合中后台 Demo；Element Plus 的组件风格适合管理端和业务系统。TypeScript 能减少接口字段错用，Pinia 能统一管理登录态。

#### 2.11.5 运用在哪些模块

- `jupin-web/src/views/player`：玩家端拼车、店铺、订单、我的。
- `jupin-web/src/views/shop`：店家端店铺、剧本库、拼车、订单。
- `jupin-web/src/views/admin`：管理员端用户和剧本管理。
- `jupin-web/src/api`：封装后端接口请求。
- `jupin-web/src/stores/auth.ts`：登录态管理。

## 第三部分：项目可以继续优化的方向

### 3.1 权限配置收口

当前 Spring Security 配置中存在较宽的 `permitAll`，虽然拦截器仍会做角色校验，但双层权限配置容易造成理解偏差。后续应改成只放行登录、注册、刷新 Token、公开列表等接口，其余接口统一要求认证，再由角色规则控制访问。

### 3.2 Redis 缓存体系

当前 Redis 已用于 Token、锁、角色抢选、拼车详情缓存和布隆过滤器。拼车详情采用 Cache Aside：先通过 `pool:detail:bloom` 判断 ID 是否可能存在，再读 `pool:detail:{poolId}`，缓存未命中时查 MySQL 并回填 Redis。

当前已经落地的缓存防护包括：

- 布隆过滤器拦截不存在的拼车 ID，减少无效请求打到 Redis/MySQL。
- 热点拼车详情不设置物理 TTL，避免热点 Key 到期瞬间大量请求打到数据库。
- 拼车状态、成员、价格、DM、确认状态等写操作后主动删除 `pool:detail:{poolId}`，保证最终一致。

后续还可以继续缓存：

- 剧本列表。
- 店铺列表。
- 热门拼车列表。
- 对布隆过滤器增加定期重建或扩容策略，避免长期运行后误判率上升。

### 3.3 数据库条件更新兜底

当前支付占座已经使用 Redisson 分布式锁 + MySQL 条件更新双重控制。Redisson 负责让同一拼车下的支付请求串行化，MySQL 负责做最后一致性兜底：

- 订单只允许 `PENDING -> PAID`。
- 成员只允许 `PENDING_PAYMENT -> JOINED`。
- 名额更新使用 `current_members = current_members + 1 where current_members < max_members`。
- 支付完成后重新统计正式成员数，回写 `current_members`，修复字段自增可能带来的计数漂移。

这种设计比单纯依赖分布式锁更稳，因为即使出现锁异常、重复请求或并发竞争，数据库条件也能阻止超员和重复占座。

### 3.4 定时补偿任务

当前超时兜底已经从定时扫描升级为 RabbitMQ 延迟队列 + 死信队列：

- 押金订单超时：订单逾期，待支付成员退出拼车。
- 尾款订单超时：订单逾期，信用分扣 10。
- 拼车开始超时：仍为 `OPEN` 且无人加入时自动取消。
- 成团确认/结束确认超时：满足条件时推进状态。
- 状态变更成功后写站内消息，并通过 WebSocket 推送 `/topic/pool/{poolId}`。

后续可以继续完善的是尾款订单自动生成、资金释放/退款闭环，以及确认超时中的拒绝票和申诉策略。

### 3.5 配置和密钥外置

`application.yml` 中不应长期保存数据库密码、RabbitMQ 密码、JWT secret。后续应改为环境变量或配置中心读取，避免泄漏。

### 3.6 消息异步化

当前订单超时和履约超时已经通过 RabbitMQ 异步处理，超时处理后的站内消息和 WebSocket 推送也已补齐。后续可以继续把评价提醒、匹配推荐结果、成团通知等非核心消息事件异步化，进一步减少主业务事务里的同步通知逻辑。

## 第四部分：面试可能提问

### 4.1：为什么当前没有直接拆成微服务

#### 4.1.1 微服务是什么

微服务是把一个完整系统拆成多个可以独立开发、独立部署、独立扩容的服务。每个服务负责一个相对独立的业务能力，例如用户服务、拼车服务、订单服务、消息服务、评价服务。服务之间通过 HTTP、RPC 或消息队列通信。

微服务主要解决的是大型系统中的几个问题：

- 多团队并行开发时减少代码冲突。
- 不同业务模块可以独立发布和回滚。
- 高流量模块可以单独扩容。
- 某个非核心服务故障时，不拖垮整个系统。

#### 4.1.2 为什么 JuPin 当前不适合直接拆微服务

JuPin 当前定位是 Java 后端实习项目和 Web Demo，目标是展示完整业务链路、领域建模和工程能力。这个阶段直接拆微服务，收益不明显，复杂度反而会明显上升。

如果强行拆分，会额外引入很多基础设施问题：

- 服务注册与发现，例如 Nacos、Eureka、Consul。
- API 网关，例如 Spring Cloud Gateway。
- 配置中心，例如 Nacos Config、Apollo。
- 服务间调用，例如 OpenFeign、Dubbo、gRPC。
- 分布式事务，例如 Seata、TCC、可靠消息最终一致。
- 链路追踪与日志聚合，例如 SkyWalking、Zipkin、ELK。
- 服务部署和监控，例如 Docker、Kubernetes、Prometheus。

这些技术本身有价值，但它们解决的是系统规模变大后的治理问题。当前项目最核心的问题仍然是业务规则正确、状态流转清晰、支付和拼车并发安全、缓存和权限完善。如果为了“看起来高级”过早拆微服务，反而会把注意力从核心业务转移到服务治理上。

#### 4.1.3 当前选择：模块化单体

项目当前采用的是“模块化单体”思路。也就是运行时还是一个后端应用，但代码内部按照业务边界拆分模块和服务：

- `UserService`：用户注册、登录、资料维护。
- `PoolService`：拼车创建、加入、退出、确认、状态流转。
- `OrderService`：订单创建、支付、退款、释放。
- `MessageService`：应用内消息和通知。
- `CreditService`：信用分增减和信用日志。
- `ReviewService`：评价创建和查询。
- `MatchEngine`：匹配推荐算法。

这样做的好处是：既能保持单体部署简单、事务边界清晰，又能让代码结构具备后续拆服务的基础。比如订单、消息、匹配这些模块已经有相对明确的职责边界，未来可以逐步拆出去。

相似方案包括：

- 传统单体：所有代码混在一个模块里，开发快但长期难维护。
- 模块化单体：单进程部署，内部按领域拆分，是当前项目选择。
- 微服务：多进程、多服务部署，适合团队和业务规模更大的阶段。
- 分布式单体：表面上拆了服务，但服务之间强耦合、互相同步调用，复杂度高但收益低，应避免。

#### 4.1.4 为什么选择模块化单体而不是微服务

当前业务中，拼车、成员、订单、信用分之间存在较强一致性要求。例如押金支付成功后，需要更新订单状态、成员状态、拼车人数；拼车完成后，需要释放资金、通知成员、可能创建后续车费订单。这些操作放在同一个服务和本地事务里更容易保证一致性。

如果拆成多个微服务，这些操作会变成跨服务调用，需要考虑调用失败、重试、幂等、补偿事务和最终一致性。对于当前项目来说，这些复杂度超过了收益。

所以当前更合理的选择是：

- 用模块化单体保证开发效率和事务一致性。
- 用清晰的 Service 边界体现微服务拆分意识。
- 用 RabbitMQ、Redis、状态机、分布式锁等技术先解决实际业务问题。
- 等业务规模增长后，再按边界逐步拆分。

#### 4.1.5 未来如果拆微服务，应该怎么拆

如果项目未来用户量和团队规模上来，可以按低耦合优先的原则逐步拆分，而不是一次性全部拆开。

推荐拆分顺序：

1. **消息通知服务**

   最适合优先拆。拼车满员、确认提醒、评价提醒、订单逾期提醒都可以通过 RabbitMQ 异步发送事件。消息服务消费事件后写消息表并推送 WebSocket。即使消息服务短暂失败，也不应该影响拼车和支付主流程。

2. **匹配推荐服务**

   匹配计算相对独立，后续如果加入更复杂的推荐算法、地理位置计算或机器学习模型，可以独立扩容。主业务只需要把开放拼车和玩家偏好传给匹配服务，拿回推荐结果。

3. **订单支付服务**

   订单服务边界清晰，但涉及资金、支付回调、退款和幂等，拆分时要非常谨慎。适合在支付流程成熟后拆出，并通过可靠消息或分布式事务保证订单状态和拼车成员状态最终一致。

4. **用户认证服务**

   如果未来有多个系统共用登录，例如移动端、小程序、商家后台、管理后台，可以拆成统一认证中心。当前项目只有一个后端应用，暂时没有必要单独拆。

5. **拼车核心服务**

   暂时不建议优先拆。拼车服务和成员、状态机、订单、信用分耦合度最高，是当前系统的核心事务边界。过早拆分会引入大量分布式一致性问题。

## 第三部分 压测校验
### 1 测试说明
#### 1.1 测试脚本说明
JuPin 后端的三类测试脚本：HTTP 场景压测、业务一致性校验、MQ/可靠事件校验。

1. `http/run_api_scenarios.sh`
    - 用于执行 HTTP 场景测试和轻量压测。
    - 覆盖拼车详情读链路、无 Token 直打拦截、订单创建幂等、重复支付、重复 Mock 回调。

2. `sql/validate_consistency.sql`
    - 用于压测后的业务一致性校验。
    - 检查重复订单、重复占座、订单支付状态与成员状态一致性、拼车人数漂移、晚到回调状态回滚风险等。

3. `mq/validate_reliability.sql`
    - 用于 MQ 和可靠事件链路校验。
    - 当前可校验 `payment_event`，同时预留了未来 `outbox_event` 表的检查逻辑。

#### 1.2 测试思路

这套脚本不是只看接口能不能返回 `200`，而是按“场景压测 + 业务校验 + 可靠性校验”的闭环来做。

HTTP 脚本负责模拟请求压力和异常请求：

- 拼车详情读链路：验证缓存链路在并发访问下的 P95 和 HTTP 成功数。
- 无 Token 直打创建订单：验证入口鉴权是否能拦截无资格流量。
- 订单创建幂等：同一用户、同一幂等 Key 连续创建订单，期望返回同一个订单号。
- 重复支付：同一订单连续支付两次，期望订单只支付一次，不重复占座。
- 重复 Mock 回调：同一渠道流水和回调请求重复提交，期望支付事件只处理一次。

SQL 脚本负责压测后验收业务结果：

- 接口返回 `200` 不代表业务一定正确，所以要继续查数据库。
- 重点看订单数、成员状态、拼车人数、支付事件流水是否一致。
- 核心异常计数应该为 `0`。

MQ/可靠性脚本负责验证异步链路：

- 当前项目主要检查 `payment_event` 是否有长期处理中、成功回调但订单未支付、逾期订单未释放成员等异常。
- 后续实现 Outbox 后，同一脚本会自动检查 pending、publish failed、compensation failed 等可靠消息指标。

#### 1.3 如何控制人数和并发

当前 HTTP 脚本用两个参数控制压测规模：

- `REQUESTS`：总请求数，可以理解为本轮压测的样本数。
- `CONCURRENCY`：并发数，即同时发起多少个请求。

例如：

- `REQUESTS=100 CONCURRENCY=10`：总共请求 100 次，每批最多 10 个并发。
- `REQUESTS=500 CONCURRENCY=50`：总共请求 500 次，每批最多 50 个并发。

当前脚本里的并发主要用于“拼车详情读链路”。订单创建、支付、回调这些写链路目前是顺序执行，因为它们依赖同一个用户和同一个订单号。如果后续要做多人并发加入/支付，需要准备多组玩家账号，并扩展脚本按账号池循环登录和发起请求。

建议压测档位：

| 档位 | REQUESTS | CONCURRENCY | 用途 |
|---|---:|---:|---|
| L1 | 100 | 10 | 冒烟测试，确认脚本和服务可用 |
| L2 | 300 | 30 | 轻量压力，观察接口是否稳定 |
| L3 | 500 | 50 | 中等压力，观察 P95 是否抬升 |
| L4 | 1000 | 100 | 高压测试，观察服务是否进入退化区 |

#### 1.4 验收指标

HTTP 脚本输出的关键指标：

| 指标 | 说明 | 期望 |
|---|---|---|
| `pool_detail_total` | 拼车详情总请求数 | 等于 `REQUESTS` |
| `pool_detail_http_200` | 拼车详情 HTTP 200 数 | 等于 `REQUESTS` |
| `pool_detail_p95_seconds` | 拼车详情 P95 响应时间 | 越低越好，用于 A/B 对比 |
| `unauthorized_direct_create_http_status` | 无 Token 直打创建订单返回码 | 不应为 `200` |
| `write_user_phone` | 写链路使用的临时玩家手机号 | 用于追踪本轮测试产生的数据 |
| `write_pool_id` | 写链路创建的临时车局 ID | 后续订单、支付、回调都基于它执行 |
| `created_order_no` | 幂等创建得到的订单号 | 两次创建应相同，脚本会自动校验 |
| `duplicate_pay_order_no` | 重复支付订单号 | 两次支付都应返回业务成功 |
| `mock_callback_channel_txn_id` | 重复回调使用的渠道流水 | 数据库中应只处理一次 |

业务一致性 SQL 的核心验收项：

| 校验项 | 期望 |
|---|---:|
| `duplicate_order_by_user_idempotent_key` | 0 |
| `duplicate_order_by_user_pool_type` | 0 |
| `duplicate_pool_member_by_pool_user` | 0 |
| `paid_order_member_not_joined` | 0 |
| `joined_member_without_paid_deposit` | 0 |
| `pool_current_members_drift` | 0 |
| `overdue_order_member_still_pending_payment` | 0 |
| `duplicate_payment_event_key` | 0 |
| `duplicate_payment_request_no` | 0 |
| `duplicate_payment_channel_txn_id` | 0 |

MQ/可靠性 SQL 的核心验收项：

| 校验项 | 期望 |
|---|---:|
| `payment_event_processing_stuck_over_5_min` | 0 |
| `payment_event_success_order_not_paid` | 0 |
| `payment_event_ignored_order_paid` | 0 |
| `overdue_deposit_member_not_released` | 0 |
| `outbox_pending_over_5_min` | 0 |
| `outbox_publish_failed` | 0 |
| `outbox_compensation_failed` | 0 |

#### 1.5 结果在哪里看

HTTP 脚本结果会直接输出在终端，例如：

```text
pool_detail_total=100
pool_detail_http_200=100
pool_detail_p95_seconds=0.0182
unauthorized_direct_create_http_status=401
created_order_no=...
duplicate_pay_order_no=...
mock_callback_channel_txn_id=...
```

拼车详情每次请求的原始耗时会写入临时文件：

```text
/tmp/jupin_pool_detail_${RUN_ID}.txt
```

每行格式是：

```text
响应耗时秒数 HTTP状态码
```

SQL 校验结果会直接输出在 MySQL 客户端。每一行包含：

- `check_name`：校验项名称。
- `anomaly_count`：异常数量。

验收时重点看 `anomaly_count` 是否为 `0`。

#### 1.6 运行 HTTP 场景脚本

前置条件：

- 后端服务已启动，默认地址是 `http://localhost:8080`。
- 本机已安装 `curl` 和 `jq`。
- `POOL_ID` 只用于拼车详情读链路压测，对应接口需要能正常返回 `HTTP 200`。
- 写链路会自动准备临时玩家和临时车局，不需要手工把种子账号加入车局。

示例：

```bash
BASE_URL=http://localhost:8080 \
PLAYER_PHONE=13812340001 \
PLAYER_PASSWORD=player123 \
POOL_ID=1 \
REQUESTS=100 \
CONCURRENCY=10 \
bash Test/http/run_api_scenarios.sh
```

也可以直接传入已经登录得到的 Token：

```bash
PLAYER_TOKEN=eyJ... POOL_ID=1 ./Test/http/run_api_scenarios.sh
```

#### 1.7 运行业务一致性校验 SQL

HTTP 测试完成后执行：

```bash
mysql -h127.0.0.1 -uroot -p jupin < Test/sql/validate_consistency.sql
```

核心异常计数期望为 `0`，尤其是：

- `duplicate_order_by_user_idempotent_key`
- `duplicate_order_by_user_pool_type`
- `paid_order_member_not_joined`
- `joined_member_without_paid_deposit`
- `pool_current_members_drift`
- `duplicate_payment_event_key`

#### 1.8 运行 MQ/可靠性校验 SQL

超时、重复回调或 MQ 相关测试完成后执行：

```bash
mysql -h127.0.0.1 -uroot -p jupin < Test/mq/validate_reliability.sql
```

当前项目可以校验 `payment_event` 可靠性。Outbox 检查使用动态 SQL 做了表存在判断，所以在 Outbox 表还没实现前也可以运行。

### 2 测试结果
记录优化前后、不同并发人数下的结果。

#### 2.1 L1 冒烟测试：100 请求 / 10 并发

测试命令：

```bash
BASE_URL=http://localhost:8080 \
PLAYER_PHONE=13812340001 \
PLAYER_PASSWORD=player123 \
POOL_ID=1 \
REQUESTS=100 \
CONCURRENCY=10 \
bash Test/http/run_api_scenarios.sh
```

测试输出：

```text
[1/5] pool detail read load: requests=100 concurrency=10
pool_detail_total=100
pool_detail_http_200=100
pool_detail_p95_seconds=0.0713
[2/5] unauthorized direct order create should be rejected
unauthorized_direct_create_http_status=401
write_user_phone=13603173738
write_pool_id=32
[3/5] idempotent order create
created_order_no=2062106431301226496
idempotent_key=test-20260603173738-pool-32-type-0
[4/5] duplicate pay
duplicate_pay_order_no=2062106431301226496
[5/5] duplicate Mock pay callback
mock_callback_order_no=2062106431301226496
mock_callback_channel_txn_id=mock-channel-2062106431301226496-20260603173738
All HTTP scenarios completed.
```

结果分析：

- 读链路 `pool_detail_http_200=100/100`，说明拼车详情接口在 10 并发、100 次请求下没有再出现 HTTP 500。
- 拼车详情 `P95=0.0713s`，可作为后续缓存优化、A/B 压测的当前基线。
- 无 Token 直打创建订单返回 `401`，说明入口鉴权能够拦截未登录流量。
- 写链路使用临时玩家 `13603173738` 和临时车局 `32`，避免种子账号已有成员状态影响测试。
- 同一幂等 Key 两次创建订单返回同一订单号 `2062106431301226496`，幂等创建场景通过。
- 重复支付和重复 Mock 回调均返回业务成功，HTTP 场景层面验证通过。

下一步：

- 已执行 `Test/sql/validate_consistency.sql`，重复订单、重复成员、支付状态与成员状态、拼车人数漂移等异常计数均为 `0`。
- 已执行 `Test/mq/validate_reliability.sql`，`payment_event` 卡处理中、成功回调但订单未支付、逾期押金成员未释放等异常计数均为 `0`。
- 后续进入 L2/L3 档位压测；若出现异常，先按异常项回查本轮 `write_user_phone`、`write_pool_id`、`created_order_no`。

业务一致性校验结果：

| 校验项 | 异常数 |
|---|---:|
| `duplicate_order_by_user_idempotent_key` | 0 |
| `duplicate_order_by_user_pool_type` | 0 |
| `duplicate_pool_member_by_pool_user` | 0 |
| `paid_order_member_not_joined` | 0 |
| `joined_member_without_paid_deposit` | 0 |
| `pool_current_members_drift` | 0 |
| `overdue_order_member_still_pending_payment` | 0 |
| `late_callback_rollback_overdue_order` | 0 |
| `duplicate_payment_event_key` | 0 |
| `duplicate_payment_request_no` | 0 |
| `duplicate_payment_channel_txn_id` | 0 |
| `paid_order_without_success_payment_event` | 0 |

MQ/可靠性校验结果：

| 校验项 | 异常数 |
|---|---:|
| `payment_event_processing_stuck_over_5_min` | 0 |
| `payment_event_success_order_not_paid` | 0 |
| `payment_event_ignored_order_paid` | 0 |
| `overdue_deposit_member_not_released` | 0 |
| `outbox_pending_over_5_min` | 0 |
| `outbox_publish_failed` | 0 |
| `outbox_compensation_failed` | 0 |
| `duplicate_outbox_event_key` | 0 |

#### 2.2 严格 A/B 压测计划

当前 `2.1` 的结果属于 L1 冒烟测试，证明链路可用、接口无 500、业务校验无异常；它可以作为当前基线，但还不能直接作为“优化收益”结论。严格 A/B 需要把读链路性能对比和写链路正确性验证拆开。

A/B 版本定义：

- A 版本：优化前 commit，例如拼车详情缓存防护升级前的版本。
- B 版本：优化后 commit，例如当前实现。
- 两个版本都必须记录 commit hash，避免用“优化前/优化后”这种不可复现描述。

执行口径：

- 每轮测试前执行 `Test/ab/reset_state.sh`，重建数据库并清空 Redis。
- 读链路性能压测使用 `Test/ab/run_read_load.sh`，只请求 `/api/player/pool/{POOL_ID}`，不执行注册、发布车局、下单、支付等写入操作。
- 每个版本至少执行 3 轮，交错执行 A/B，降低机器温度、后台任务、JIT 预热等时间因素影响。
- 主指标取 `p95_seconds` 中位数，辅助观察 `p99_seconds` 和 `throughput_req_s`。
- 若任一轮 `http_200` 不等于 `requests`，该轮不参与性能结论，先修正确性问题。

单轮命令：

```bash
bash Test/ab/reset_state.sh
LABEL=A_round1 \
BASE_URL=http://localhost:8080 \
POOL_ID=1 \
REQUESTS=300 \
CONCURRENCY=30 \
bash Test/ab/run_read_load.sh
```

建议执行顺序：

```text
A_round1 -> reset -> B_round1 -> reset
B_round2 -> reset -> A_round2 -> reset
A_round3 -> reset -> B_round3
```

结果位置：

- `Test/results/${RUN_ID}_${LABEL}_read_raw.tsv`：每次请求的耗时和 HTTP 状态码。
- `Test/results/${RUN_ID}_${LABEL}_read_summary.tsv`：本轮汇总指标。

#### 2.3 第三版严格 A/B 冒烟测试发现的问题

第三版测试方案改为以 `cleanup-bench-review` 作为完整优化分支，再分别构造三个只移除单一优化点的基线分支：

- `ab-baseline-oversell`：移除支付占座防护。
- `ab-baseline-idempotent`：移除订单创建和支付回调幂等保护。
- `ab-baseline-cache`：移除拼车详情缓存。

每个 A/B 轮次开始前执行 `Test/ab/reset_state.sh`，重建 MySQL 数据并清空 Redis，避免用户、车局、订单、幂等 Key 和缓存跨轮污染。

本轮幂等冒烟测试暴露了一个并发窗口问题：

```text
A 分支（无幂等保护）：
create:   ok=1,  fail=49, distinct_orders=1
callback: ok=1,  fail=49

B 分支（原优化实现）：
create:   ok=41, fail=9,  distinct_orders=1
callback: ok=41, fail=9
```

根因是唯一索引已经阻止了重复落库，但 `DuplicateKeyException` 后立即回查已有订单或支付事件时，赢得唯一索引竞争的事务可能尚未提交，输掉竞争的请求短时间内查不到记录，于是返回 500。

修复策略：

- 订单创建捕获唯一索引冲突后短重试回查，最多 5 次，每次间隔 30ms。
- 回查顺序优先 `user_id + idempotent_key`，再用 `user_id + pool_id + type` 兜底，覆盖 `uk_user_idempotent_key` 和 `uk_user_pool_type` 两类唯一索引触发场景。
- Mock 回调支付事件捕获唯一索引冲突后，按 `event_key`、`channel_txn_id`、`request_no` 三个唯一键短重试回查。
- 由于 MySQL 默认 `REPEATABLE READ` 下同一事务的普通查询会复用快照，重复键后的短重试仍可能看不到刚提交的赢家事务记录，所以订单创建和 Mock 回调事务隔离级别调整为 `READ_COMMITTED`。
- 重试后仍查不到才继续抛异常，避免吞掉真实异常。

修复后的预期结果：

```text
B 分支：
create:   ok=50, fail=0, distinct_orders=1
callback: ok=50, fail=0
```

### 3 测试中发现的错误及其修复
每次测试若是不通过，则说明如何追踪确定错误、错误是什么、怎么修改。

#### 3.1 详情接口 HTTP 500：VO 字段类型不一致

现象：

- HTTP 脚本第一步拼车详情读链路压测中，`pool_detail_total=100`，但 `pool_detail_http_200=0`。
- 单独请求 `/api/player/pool/1` 返回 `{"code":500,"message":"服务器内部错误","data":null}`。

追踪方式：

- 先对比 `/api/player/pool/list` 和 `/api/player/pool/1`，发现列表接口正常，详情接口异常，说明数据库和基础查询不是主要问题。
- 再查看 Docker 后端日志，定位到异常堆栈在 `VOConverter.toPoolVO`：

```text
cn.hutool.core.convert.ConvertException: Unsupported source type: class java.lang.String
at com.jupin.server.converter.VOConverter.toPoolVO(VOConverter.java:22)
```

原因：

- `CarPool.roles` 是数据库中的字符串 JSON。
- `PoolVO.roles` 是 `List<RoleStatusVO>`。
- 详情接口转换 VO 时直接使用 Bean 拷贝，Hutool 尝试把字符串 JSON 自动转换成 `List<RoleStatusVO>`，转换失败后导致接口返回 500。

修复：

- 将 `VOConverter.toPoolVO` 从自动 Bean 拷贝改为显式字段映射。
- 普通拼车详情 VO 不再直接拷贝 `roles` 字段，角色状态仍由独立的 `/api/player/pool/{poolId}/roles` 链路处理。

修复效果：

- 后端重启后，`/api/player/pool/1` 不再因 `roles` 类型转换失败返回 500。
- L1 冒烟测试中，拼车详情读链路 `100/100` 请求返回 HTTP 200。

#### 3.2 押金订单创建失败：测试账号业务状态不满足

现象：

- HTTP 脚本第三步“幂等订单创建”失败。
- 返回信息为 `{"code":500,"message":"成员状态不允许创建押金订单","data":null}`。

追踪方式：

- 查询种子数据和数据库后发现，`13812340001` 在 `POOL_ID=1` 中已经是正式成员。
- 后端 `OrderServiceImpl.create` 对押金订单有业务约束：只有 `PENDING_PAYMENT` 成员才能创建押金订单。

原因：

- 原脚本把 `POOL_ID=1` 同时用于“读详情压测”和“写订单链路测试”。
- 但种子账号在 1 号车局里已是 `JOINED` 状态，不满足押金订单创建前置条件。
- 这个报错不是后端业务错误，而是测试脚本构造的业务上下文不正确。

修复：

- 调整 HTTP 脚本，将读链路和写链路拆开。
- `POOL_ID` 只用于拼车详情读链路压测。
- 写链路自动注册或登录临时玩家，并发布临时玩家局，再对临时车局执行幂等下单、重复支付和重复回调。
- 脚本输出 `write_user_phone`、`write_pool_id`，便于压测后回查本轮测试数据。

修复效果：

- L1 冒烟测试中，脚本自动创建临时玩家 `13603173738`、临时车局 `32`。
- 幂等下单返回订单号 `2062106431301226496`，重复下单返回同一订单号。
- 重复支付和重复 Mock 回调均完成，HTTP 场景通过。
